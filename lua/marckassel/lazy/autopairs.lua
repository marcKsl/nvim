return {
    'windwp/nvim-autopairs',
    event = "InsertEnter",
    config = true,
    -- use opts = {} for passing setup options
    -- this is equivalent to setup({})
    opts = {
        -- Hier kannst du zusätzliche Optionen für nvim-autopairs einfügen
    },
}
